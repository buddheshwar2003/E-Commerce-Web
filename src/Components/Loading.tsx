
const Loading = () => {
  return (
    <section className="fixed inset-0 bg-black bg-opacity-80 flex justify-center items-center">
        <div className="h-40 w-40 rounded-full border-[10px] border-blue-500 border-t-transparent animate-spin duration-300"></div>
    </section>
  )
}

export default Loading